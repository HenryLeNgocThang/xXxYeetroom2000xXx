@model xXxYeetroom2000xXx.Models.Kommentar

@{
    ViewData["Title"] = "Create Post";
}

<h1>Create Post</h1>

<h4>Post</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="CreateKommentar">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Verfasser" class="control-label"></label>
                <input asp-for="Verfasser" class="form-control" />
                <span asp-validation-for="Verfasser" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Eintrag" class="control-label"></label>
                <input asp-for="Eintrag" class="form-control" />
                <span asp-validation-for="Eintrag" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="CreateKommentar" class="btn btn-primary" />
            </div>
            <input asp-for="Post_ID" value="@ViewBag.PostID" class="form-control" />
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
